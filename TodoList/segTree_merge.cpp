#include <iostream>
#include <string>
#include <map>
#include <queue>
#include <vector>
#include <algorithm>
#include <cstring>
#include <cstdlib>
#include <ctime>

using namespace std;

pair<int,int>

pii max(pii x,y){
	if(x.f!=y.f)return x.f>y.f?x:y;
	else return x.
}

pushup(){
	now=max(lll,rrr);
}



void merge(int &x,int y,int l,int r){
	if(!x||!y)x|=y;
	else if(l==r){
		t[x].val+=t[y].val;
	} 
	else{
		int mid;
		merge(llll);
		merge(rrrr);
		pushup(x);
	}
}

int main ()
{

	return 0;
}

