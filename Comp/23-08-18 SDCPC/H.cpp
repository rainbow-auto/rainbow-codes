#include <iostream>

using namespace std;

const int maxn = 5005;

int N, M;
int a[maxn], b[maxn];

inline void lsh () {
    vector<int> l;
    for (int i = 1; i <= k; i++) { l.push_back (a[i]); l.push_back (b[i]); }
    sort (l.begin(), l.end());
    int lsiz = unique (l.begin(), l.end()) - l.begin();

    for (int i = 1; i <= n; i++) [
        
    ]
}

int main () {

    return 0;
}