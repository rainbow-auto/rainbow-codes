#include <iostream>

int n, q;
std::set <int> a[maxn];

int main () {

    std::cin >> n;

    for (int i = 1; i <= n; i++) {
        int c; std::cin >> c;
        while (c--) { int x; std::cin >> x; a[i].insert (x); }
    }

    for (int i = 1; i <= n; i++) {
        
    }

    std::cin >> q;
    while (q--) {
        int
    }

    return 0;
}