#include <iostream>

const int maxn = 20005;

int n, r;
int a[maxn];

int main () {

    std::cin >> n >> r;

    for (int i = 1; i <= n; i++) { std::cin >> a[i]; }

    for (int i = 1; i <= n; i++) {
        if (a[i] == 1) { continue; }
        
    }

    return 0;
}